<!DOCTYPE html>
<html>

<head>
    <title>Hunter</title>
    <!-- Google Icon Fonts used by Materialize -->
    <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- Materialize CSS Framework -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="stylesheets/edit.css" />
    <!-- JavaScript for Materialize -->
    <script defer src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</head>

<body>
    <div class="navbar-fixed">
        <nav>
            <div class="nav-wrapper">
                <a href="" class="brand-logo left">Item Descriptions</a>
                <ul class="right">
                    <li>
                        <% if (user) { %>
                        <a href="/logout"><i class="material-icons left"></i>Log Out</a>
                        <% } else { %>
                        <a href="/auth/google"><i class="material-icons left">vpn_key</i>Login with Google</a>
                        <% } %>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
    <form id="placeholders" action="/items/<%= item._id %>/edit" method="POST">
        <input type="text" name="title" value="<%= item.title %>" placeholder="title">
        <input type="text" name="description" value="<%= item.description %>" placeholder="description">
        <input type="text" name="price" value="<%= item.price %>" placeholder="price" placeholder="paste in image url">
        <input type="text" name="image" value="<%= item.image %>" placeholder=" paste in Image URL">
        <input type="submit" value='SUBMIT'>
    </form>
    <form action="/items/<%= item._id %>?_method=DELETE" method="POST">
        <input type="submit" value="  DELETE YOUR ITEM">
    </form>
   