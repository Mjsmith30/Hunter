<!DOCTYPE html>
<html>

<head>
  <title>Hunter</title>
  <!-- Google Icon Fonts used by Materialize -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <!-- Materialize CSS Framework -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link rel="stylesheet" href="stylesheets/homepage.css" />
  <!-- JavaScript for Materialize -->
  <script defer src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</head>

<body>
  <div class="navbar-fixed">
    <nav>
      <div class="nav-wrapper">
        <a id ="logo" href="/items" class="brand-logo left">THRIFT SHOP X HUNTER</a>
        <ul class="right">
          <li>
          <% if (user) { %>
              <a id ="logout" href="/logout"><i class="material-icons left">vpn_key</i>Log Out</a>
          <% } else { %>
            <a id = "login" href="/auth/google"><i class="material-icons left">vpn_key</i>Login with Google</a>
            <% } %>
          </li>
        </ul>
      </div>
    </nav>
  </div>
  <div class="container">
      <% if (user) { %>
    <a id = "profile" href="/hunter/<%= user._id%>"> <i class="material-icons left">vpn_key</i>VISIT HUNTER PROFILE PAGE</a>
    <% } else { %>
      <% } %>
    <div id="search-row" class="row">
      <h2 class="col s4 ">Hunters</h2>
      <form action="/hunter" method="GET">
        <div class="col s3 offset-s3">
          <!-- search -->
          <div class="input-field">
          </div>
        </div>
        <div class="col s2">
        </div>
      </form>
    </div>
    <div class="row">
      <div id="hunter" class="col s12">
        <% hunters.forEach(function(hunter) { %>
          <div class="card blue-grey">
            <div class="card-content black-text">
              <div>
                <span class="card-title">
                  <i class="small material-icons">perm_identity</i>
                  <span class="hunter-name"><%= hunter.name %></span>
                  <% if (hunter.item) { %><span class="badge white">SEI <%= hunter.item %></span><% } %>
                </span>
              </div>
              <ul class="collection">
                <% hunter.items.forEach(function(i) { %>
                  <li class="collection-item blue-grey-text text-darken-2"><%- i.text %></li>
                <% }) %>
              </ul>
              <% if (hunter._id.equals(user && user._id)) { %>
                <!-- <div class="card-action">
                  <form action="/items" method="POST">
                    <input type="text" name="text" class="white-text">
                    <button type="submit" class="btn white-text">Add something to 'Hunt' for</button>
                  </form>
                </div> -->
                <a href="/items/new">Add new Item</a>
              <% } %>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>
</body>

</html>
